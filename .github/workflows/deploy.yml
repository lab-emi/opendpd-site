name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:
    inputs:
      studio_ref:
        description: 'OpenDPD commit or tag to publish under /studio/'
        required: false
        type: string

# 必须加上这些权限，否则 403 报错
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Checkout the reviewed Studio source
        uses: actions/checkout@v4
        with:
          repository: lab-emi/OpenDPD
          ref: ${{ inputs.studio_ref || vars.STUDIO_REF || '290e1d4603727d59f417ff4dc773a54c42065a84' }}
          path: .studio-source

      - name: Build the public Studio
        working-directory: .studio-source/frontend
        env:
          VITE_STUDIO_MODE: web
          VITE_API_ORIGIN: https://api.opendpd.com
          VITE_BASE_PATH: ./
        run: npm ci && npm run build

      - name: Include Studio without replacing the landing page
        run: |
          mkdir -p dist/studio
          cp -a .studio-source/frontend/dist/. dist/studio/
          touch dist/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
